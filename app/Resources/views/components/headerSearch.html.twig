{{ form_start(form) }}
<form name="headerSearch" action="">
    <ul>
        <li>
            {{ form_widget(form.texts, {'attr': {
                'class': 'form-control',
                'placeholder': "Ecrivez un ou plusieurs mots-clés dans la barre de recherche"
            } }) }}
        </li>
        <li>
            <button type="submit" class="btn"><i class="fa fa-search"></i></button>
        </li>
    </ul>
</form>
{{ form_widget(form._token) }}
{{ form_end(form, {'render_rest': false}) }}
